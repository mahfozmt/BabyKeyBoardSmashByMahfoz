<Window x:Class="BabyKeyBoardSmash.Installer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="বেবি কীবোর্ড স্ম্যাশ — ইনস্টলার (BabyKeyBoardSmash Setup)"
        Icon="app.png"
        Height="500" Width="630"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#1E1E2E"
        Foreground="#F8FAFC"
        FontFamily="Segoe UI, Kalpurush, sans-serif">

    <Window.Resources>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#6366F1"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="24,10"/>
            <Setter Property="MinWidth" Value="180"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#4F46E5"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#4338CA"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#3B4261"/>
                                <Setter Property="Foreground" Value="#717C9B"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#313244"/>
            <Setter Property="Foreground" Value="#CDD6F4"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="MinWidth" Value="130"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#45475A"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#585B70"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#282A36" CornerRadius="12" Padding="16,14" Margin="0,0,0,16" BorderBrush="#3B4261" BorderThickness="1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Border Width="50" Height="50" CornerRadius="12" ClipToBounds="True" VerticalAlignment="Center" Margin="0,0,14,0" Background="#313244">
                    <Image Source="app.png" Width="50" Height="50" RenderOptions.BitmapScalingMode="HighQuality"/>
                </Border>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="বেবি কীবোর্ড স্ম্যাশ (BabyKeyBoardSmash)" FontSize="18" FontWeight="Bold" Foreground="#F8FAFC"/>
                    <TextBlock Text="ছোটদের জন্য ফুলস্ক্রিন নিরাপদ বাংলা বর্ণ ও সংখ্যা শেখার অ্যাপ" FontSize="12" Foreground="#A6ADC8" Margin="0,3,0,0"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Configuration Card -->
        <Border Grid.Row="1" Background="#252538" CornerRadius="12" Padding="18" BorderBrush="#3B4261" BorderThickness="1">
            <StackPanel VerticalAlignment="Center">
                <!-- Target Directory -->
                <TextBlock Text="ইনস্টলেশন ফোল্ডার (Install Location):" FontSize="13" FontWeight="SemiBold" Foreground="#CDD6F4" Margin="0,0,0,6"/>
                <Grid Margin="0,0,0,14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="TxtInstallPath" Grid.Column="0"
                             Background="#181825" Foreground="#F8FAFC"
                             BorderBrush="#45475A" BorderThickness="1"
                             ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"
                             FontSize="12" Padding="10,7" VerticalContentAlignment="Center"/>
                    <Button x:Name="BtnBrowse" Grid.Column="1" Margin="8,0,0,0"
                            Content="ব্রাউজ (Browse)" Style="{StaticResource SecondaryButton}"
                            Padding="14,6" MinWidth="110" FontSize="12" Click="BtnBrowse_Click"/>
                </Grid>

                <!-- Options -->
                <TextBlock Text="বিকল্পসমূহ (Options):" FontSize="13" FontWeight="SemiBold" Foreground="#CDD6F4" Margin="0,0,0,8"/>
                <CheckBox x:Name="ChkDesktopShortcut" Content="ডেস্কটপ শর্টকাট তৈরি করুন (Create Desktop Shortcut)"
                          IsChecked="True" Margin="0,0,0,8" FontSize="13" Foreground="#CDD6F4"/>
                <CheckBox x:Name="ChkStartMenuShortcut" Content="স্টার্ট মেনু শর্টকাট তৈরি করুন (Create Start Menu Shortcut)"
                          IsChecked="True" Margin="0,0,0,8" FontSize="13" Foreground="#CDD6F4"/>
                <CheckBox x:Name="ChkLaunchAfter" Content="ইনস্টলেশন শেষে অ্যাপ চালু করুন (Launch after install)"
                          IsChecked="True" FontSize="13" Foreground="#CDD6F4"/>
            </StackPanel>
        </Border>

        <!-- Progress Area -->
        <StackPanel Grid.Row="2" Margin="0,14,0,14">
            <TextBlock x:Name="TxtStatus" Text="ইনস্টল করতে প্রস্তুত (Ready to install)" FontSize="12" Foreground="#BAC2DE" Margin="0,0,0,6"/>
            <ProgressBar x:Name="PbProgress" Height="8" Minimum="0" Maximum="100" Value="0"
                         Background="#181825" Foreground="#6366F1" BorderThickness="0"/>
        </StackPanel>

        <!-- Action Buttons -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Mahfoz © 2026" FontSize="11" Foreground="#6C7086" VerticalAlignment="Center"/>

            <Button x:Name="BtnCancel" Grid.Column="2" Content="বাতিল (Cancel)"
                    Style="{StaticResource SecondaryButton}" Margin="0,0,12,0"
                    Click="BtnCancel_Click"/>
            <Button x:Name="BtnInstall" Grid.Column="3" Content="ইনস্টল করুন (Install)"
                    Style="{StaticResource ModernButton}"
                    Click="BtnInstall_Click"/>
        </Grid>
    </Grid>
</Window>
